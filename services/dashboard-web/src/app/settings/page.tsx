import { Placeholder } from '@/components/Placeholder';
export default function Page() {
  return (
    <Placeholder
      title="Settings"
      sub="System configuration, tokens and provider routing."
      phase="Phase 2"
      note="Admin-controlled settings (LLM routing, thresholds, approval policies) will be editable here."
    />
  );
}
