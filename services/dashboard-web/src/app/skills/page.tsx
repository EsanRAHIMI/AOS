import { Placeholder } from '@/components/Placeholder';
export default function Page() {
  return (
    <Placeholder
      title="Skills"
      sub="Repeated successful patterns promoted into reusable skills."
      phase="Phase 3"
      note="The skill library is populated by the memory-agent once enough successful runs accumulate."
    />
  );
}
