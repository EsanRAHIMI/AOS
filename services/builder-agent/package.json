{
  "name": "@factory/builder-agent",
  "version": "0.1.0",
  "description": "Implementation: generates and modifies code, scaffolds service folders, API endpoints, frontend components, workers and tests following existing architecture.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --env-file=.env --watch --import tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@factory/shared": "workspace:*",
    "@factory/service-kit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.15.0"
  }
}
