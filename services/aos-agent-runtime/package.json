{
  "name": "@factory/aos-agent-runtime",
  "version": "0.1.0",
  "description": "K1 Consolidation Prep (D-168): hosts the architect-agent, qa-agent, reviewer-agent, and report-agent workers as one deployable, each still bound to its historical port/domain/serviceId. Transitional/candidate — see docs/deployment-plan.md for cutover status; production topology is unchanged until Dokploy is manually repointed.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --env-file=.env --watch --import tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@factory/shared": "workspace:*",
    "@factory/service-kit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.15.0",
    "vitest": "^4.1.10"
  }
}
