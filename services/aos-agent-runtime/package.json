{
  "name": "@factory/aos-agent-runtime",
  "version": "0.1.0",
  "description": "K1 Consolidation Prep (D-168/D-172): hosts the architect-agent, qa-agent, reviewer-agent, report-agent, documentation-service, memory-agent, and internet-research-service workers as one deployable, each still bound to its historical port/domain/serviceId. CODE-LEVEL CANDIDATE ONLY — production topology unchanged. See docs/deployment-plan.md for cutover status.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --env-file=.env --watch --import tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@factory/shared": "workspace:*",
    "@factory/service-kit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "tsx": "^4.19.2",
    "@types/node": "^22.15.0",
    "vitest": "^4.1.10"
  }
}
