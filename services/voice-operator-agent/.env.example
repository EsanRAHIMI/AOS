NODE_ENV=development
FACTORY_ENV=local
FACTORY_INTERNAL_TOKEN=change-me-internal
FACTORY_ADMIN_TOKEN=

SERVICE_ID=voice-operator-agent
SERVICE_NAME=Voice Operator Agent
SERVICE_DOMAIN=https://voice.simorx.com
SERVICE_PORT=4121

SERVICE_REGISTRY_URL=http://localhost:4108
EVENT_BUS_URL=http://localhost:4111

MONGODB_URI=
MONGODB_DB_NAME=autonomous_os_kernel

# LLM (optional) — explanation reasoning; deterministic router fallback otherwise.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
LLM_DEFAULT_PROVIDER=anthropic

# Phase 18/19 — realtime voice (optional). Without these, the dock runs text + browser
# voice only and clearly shows "voice provider not configured". No key ever reaches
# the browser except a short-lived ephemeral client secret minted server-side.
# Phase 19 mints via the GA endpoint (POST /v1/realtime/client_secrets) with a
# beta fallback (POST /v1/realtime/sessions); the browser's SDP offer is proxied
# through the gateway (POST /v1/voice/realtime/sdp) using only the ephemeral secret.
VOICE_PROVIDER=openai
VOICE_MODEL=gpt-realtime
VOICE_NAME=alloy
VOICE_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
VOICE_SESSION_SECRET=change-me-voice-session
# Hard cap per realtime session (client enforces + ephemeral token expiry).
# VOICE_SESSION_MAX_SECONDS takes precedence; VOICE_MAX_SESSION_MINUTES kept for compat.
VOICE_SESSION_MAX_SECONDS=600
VOICE_MAX_SESSION_MINUTES=30
VOICE_REQUIRE_PUSH_TO_TALK=true

LOG_LEVEL=info
